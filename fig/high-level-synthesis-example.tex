\footnotesize
\begin{verbatim}

unsigned long example 
	(unsigned char a,
	 unsigned char b,
	 unsigned char c,
	 unsigned char d)
{
unsigned long r;
r = (a * b) + (c * d);
return r;
}
\end{verbatim}
\normalsize
